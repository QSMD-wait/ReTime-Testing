<Window x:Class="ReTime_Testing.Views.Settings.Setting"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReTime_Testing.Views.Settings"
        xmlns:viewModels="clr-namespace:ReTime_Testing.ViewModels"
        xmlns:pages="clr-namespace:ReTime_Testing.Views.Settings.Pages"
        xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
        mc:Ignorable="d"
        Title="设置"
        ui:WindowHelper.UseModernWindowStyle="True"
        ui:WindowHelper.SystemBackdropType="Mica"
        ui:TitleBar.Height="36"
        Width="1100"
        Height="600"
        MinWidth="600"
        MinHeight="400">

    <Window.Resources>
        <!-- 数据模板：全局设置 -->
        <DataTemplate DataType="{x:Type viewModels:GlobalPageViewModel}">
            <pages:GlobalPage/>
        </DataTemplate>

        <!-- 数据模板：进度条设置 -->
        <DataTemplate DataType="{x:Type viewModels:ProgressPageViewModel}">
            <pages:ProgressPage/>
        </DataTemplate>

        <!-- 数据模板：关于 -->
        <DataTemplate DataType="{x:Type viewModels:AboutPageViewModel}">
            <pages:AboutPage/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <ui:NavigationView x:Name="MainNavigation"
                           AlwaysShowHeader="False"
                           IsTabStop="False"
                           IsBackEnabled="False"
                           IsBackButtonVisible="Collapsed"
                           IsSettingsVisible="False"
                           PaneDisplayMode="Auto"
                           OpenPaneLength="280"
                           CompactPaneLength="48">

            <!-- 主要导航项 -->
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="基本" Tag="Global">
                    <ui:NavigationViewItem.Icon>
                        <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Home}"/>
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>

                <ui:NavigationViewItem Content="进度条" Tag="Progress">
                    <ui:NavigationViewItem.Icon>
                        <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Refresh}"/>
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
            </ui:NavigationView.MenuItems>

            <!-- 页脚导航项 -->
            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem Content="关于 ReTime - Testing" Tag="About">
                    <ui:NavigationViewItem.Icon>
                        <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Info}"/>
                    </ui:NavigationViewItem.Icon>
                </ui:NavigationViewItem>
            </ui:NavigationView.FooterMenuItems>

            <!-- 内容区域 -->
            <ui:NavigationView.Content>
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top"
                               Text="{Binding SelectedItem.Content, ElementName=MainNavigation}"
                               FontSize="24" FontWeight="Normal"
                               Margin="16,16,0,8"/>
                    <ContentControl Content="{Binding CurrentPage}"/>
                </DockPanel>
            </ui:NavigationView.Content>

        </ui:NavigationView>
    </Grid>

</Window>