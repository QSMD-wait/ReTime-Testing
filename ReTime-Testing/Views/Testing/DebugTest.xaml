<Window x:Class="ReTime_Testing.Views.Testing.DebugTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
        xmlns:pages="clr-namespace:ReTime_Testing.Views.Testing.Pages"
        xmlns:viewModels="clr-namespace:ReTime_Testing.ViewModels.Testing"
        xmlns:controls="clr-namespace:ReTime_Testing.Controls"
        mc:Ignorable="d"
        Title="调试测试"
        ui:WindowHelper.UseModernWindowStyle="True"
        ui:WindowHelper.SystemBackdropType="Mica"
        ui:TitleBar.Height="36"
        Width="1080"
        Height="700"
        MinWidth="760"
        MinHeight="520">

    <Window.Resources>
        <!-- 数据模板：首页（服务状态总览） -->
        <DataTemplate DataType="{x:Type viewModels:HomePageViewModel}">
            <pages:HomePage/>
        </DataTemplate>

        <!-- 数据模板：主功能测试 -->
        <DataTemplate DataType="{x:Type viewModels:MainFeatureViewModel}">
            <pages:MainFeaturePage/>
        </DataTemplate>

        <!-- 数据模板：服务接口调试 -->
        <DataTemplate DataType="{x:Type viewModels:ServiceDebugViewModel}">
            <pages:ServiceDebugPage/>
        </DataTemplate>

        <!-- 数据模板：控件测试 -->
        <DataTemplate DataType="{x:Type viewModels:ControlsViewModel}">
            <pages:ControlsPage/>
        </DataTemplate>

        <!-- 数据模板：抽屉控件测试 -->
        <DataTemplate DataType="{x:Type viewModels:DrawerTestViewModel}">
            <pages:DrawerTestPage/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <controls:DrawerHost IsDrawerOpen="{Binding IsDrawerOpen, Mode=TwoWay}"
                             DrawerWidth="{Binding DrawerWidth}">
            <TabControl ItemsSource="{Binding Tabs}"
                        DisplayMemberPath="TabTitle"
                        SelectedIndex="{Binding SelectedTabIndex}"
                        Style="{StaticResource TabControlPivotStyle}"
                        Padding="0"/>

            <!-- 抽屉内容 -->
            <controls:DrawerHost.DrawerContent>
                <pages:DrawerTestContentPage DataContext="{Binding DrawerTest}"/>
            </controls:DrawerHost.DrawerContent>
        </controls:DrawerHost>

        <!-- Toast 通知容器 -->
        <controls:ToastOverlay x:Name="ToastOverlayControl"/>
    </Grid>
</Window>