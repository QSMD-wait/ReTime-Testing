<Window x:Class="ReTime_Testing.Views.Testing.LogViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
        mc:Ignorable="d"
        Title="日志查看器"
        ui:WindowHelper.UseModernWindowStyle="True"
        ui:WindowHelper.SystemBackdropType="Mica"
        ui:TitleBar.Height="36"
        Width="1000"
        Height="640"
        MinWidth="700"
        MinHeight="420">

    <Grid Margin="16,16,16,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 过滤栏 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="等级筛选" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,16,0"/>
                <CheckBox Content="错误" IsChecked="{Binding IsErrorFilterEnabled}" Margin="0,0,16,0" VerticalAlignment="Center"/>
                <CheckBox Content="警告" IsChecked="{Binding IsWarningFilterEnabled}" Margin="0,0,16,0" VerticalAlignment="Center"/>
                <CheckBox Content="信息" IsChecked="{Binding IsInfoFilterEnabled}" Margin="0,0,16,0" VerticalAlignment="Center"/>
                <CheckBox Content="调试" IsChecked="{Binding IsDebugFilterEnabled}" Margin="0,0,16,0" VerticalAlignment="Center"/>
                <CheckBox Content="跟踪" IsChecked="{Binding IsTraceFilterEnabled}" VerticalAlignment="Center"/>
            </StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="关键字" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,16,0"/>
                <TextBox Grid.Column="1"
                         Text="{Binding Keyword, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center"
                         ToolTip="按日志内容或来源模块过滤"/>
            </Grid>
        </StackPanel>

        <!-- 日志列表（已禁用列排序，标题不可点击） -->
        <DataGrid x:Name="LogGrid"
                  Grid.Row="1"
                  ItemsSource="{Binding Entries}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserResizeRows="False"
                  CanUserSortColumns="False"
                  CanUserReorderColumns="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None"
                  RowHeaderWidth="0"
                  SelectionMode="Extended"
                  BorderThickness="0"
                  Background="Transparent"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            <DataGrid.Columns>
                <DataGridTextColumn Header="时间" Width="100" IsReadOnly="True">
                    <DataGridTextColumn.Binding>
                        <Binding Path="Timestamp" StringFormat="HH:mm:ss.fff"/>
                    </DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontFamily" Value="Consolas"/>
                            <Setter Property="FontSize" Value="12"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="等级" Width="52" IsReadOnly="True"
                                    Binding="{Binding Level}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontSize" Value="12"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Level}" Value="ERR">
                                    <Setter Property="Foreground" Value="Red"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Level}" Value="WRN">
                                    <Setter Property="Foreground" Value="Orange"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Level}" Value="INF">
                                    <Setter Property="Foreground" Value="Green"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Level}" Value="DBG">
                                    <Setter Property="Foreground" Value="Gray"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Level}" Value="TRC">
                                    <Setter Property="Foreground" Value="Gray"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="来源" Width="160" IsReadOnly="True"
                                    Binding="{Binding Module}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Foreground" Value="Blue"/>
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="内容" Width="*" IsReadOnly="True"
                                    Binding="{Binding Message}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 操作栏 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
            <CheckBox Content="自动滚动" IsChecked="{Binding AutoScroll}" VerticalAlignment="Center" Margin="0,0,16,0"/>
            <Button Content="复制选中"
                    Command="{Binding CopySelectedCommand}"
                    CommandParameter="{Binding SelectedItems, ElementName=LogGrid}"
                    Width="100" Height="32" Margin="0,0,8,0"
                    ToolTip="复制选中日志到剪贴板"/>
            <Button Content="清空"
                    Command="{Binding ClearLogsCommand}"
                    Width="100" Height="32" Margin="0,0,8,0"
                    ToolTip="清空日志查看器缓冲"/>
            <Button Content="打开日志目录"
                    Command="{Binding OpenLogDirectoryCommand}"
                    Width="120" Height="32"
                    ToolTip="打开日志文件所在目录"/>
        </StackPanel>
    </Grid>
</Window>