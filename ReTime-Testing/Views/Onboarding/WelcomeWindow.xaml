<Window x:Class="ReTime_Testing.Views.Onboarding.WelcomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:pages="clr-namespace:ReTime_Testing.Views.Onboarding.WelcomePages"
        xmlns:helpers="clr-namespace:ReTime_Testing.Helpers"
        xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
        xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
        mc:Ignorable="d"
        Title="欢迎使用 ReTime - Testing"
        ui:WindowHelper.UseModernWindowStyle="True"
        ui:WindowHelper.SystemBackdropType="Mica"
        ui:TitleBar.Height="36"
        ui:TitleBar.IsIconVisible="False"
        Width="840"
        Height="600"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <helpers:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 引导步骤内容 -->
        <ContentControl Grid.Row="0" Content="{Binding}">
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Setter Property="ContentTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <pages:WelcomePage/>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding CurrentIndex}" Value="1">
                            <Setter Property="ContentTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <pages:ThemePage/>
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding CurrentIndex}" Value="2">
                            <Setter Property="ContentTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <pages:AutoStartPage/>
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding CurrentIndex}" Value="3">
                            <Setter Property="ContentTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <pages:CalibrationPage/>
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding CurrentIndex}" Value="4">
                            <Setter Property="ContentTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <pages:ConfirmPage/>
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding CurrentIndex}" Value="5">
                            <Setter Property="ContentTemplate">
                                <Setter.Value>
                                    <DataTemplate>
                                        <pages:DonePage/>
                                    </DataTemplate>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>

        <!-- 底部导航栏 -->
        <Border Grid.Row="1"
                Padding="24,12"
                BorderThickness="0,1,0,0"
                BorderBrush="{DynamicResource DividerStrokeColorDefaultBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 步骤指示 -->
                <TextBlock Grid.Column="0"
                           Text="{Binding StepText}"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>

                <!-- 导航按钮 -->
                <ikw:SimpleStackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                    <Button Content="上一步"
                            MinWidth="96"
                            Command="{Binding BackCommand}"
                            IsEnabled="{Binding CanGoBack}"/>
                    <Button Content="下一步"
                            MinWidth="96"
                            Command="{Binding NextCommand}"
                            IsEnabled="{Binding CanGoNext}"
                            Style="{StaticResource AccentButtonStyle}"
                            Visibility="{Binding IsLastPage, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                    <Button Content="完成"
                            MinWidth="96"
                            Command="{Binding FinishCommand}"
                            Style="{StaticResource AccentButtonStyle}"
                            Visibility="{Binding IsLastPage, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </ikw:SimpleStackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>