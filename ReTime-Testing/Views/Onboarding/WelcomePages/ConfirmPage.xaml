<UserControl x:Class="ReTime_Testing.Views.Onboarding.WelcomePages.ConfirmPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
             xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="840">

    <Grid>
        <ikw:SimpleStackPanel Margin="170,0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    Spacing="4">

            <TextBlock Text="确认选项"
                       FontSize="26"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8"/>

            <TextBlock Text="您已选择以下配置，请确认无误后点击「下一步」。"
                       FontSize="13"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       Margin="0,0,0,24"/>

            <ui:SettingsCard Header="应用主题"
                             HorizontalAlignment="Stretch">
                <ui:SettingsCard.HeaderIcon>
                    <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.DarkTheme_24_Regular}" />
                </ui:SettingsCard.HeaderIcon>
                <TextBlock Text="{Binding SelectedThemeText}" VerticalAlignment="Center"/>
            </ui:SettingsCard>

            <ui:SettingsCard Header="开机自启"
                             HorizontalAlignment="Stretch">
                <ui:SettingsCard.HeaderIcon>
                    <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.ArrowRepeatAll_24_Regular}" />
                </ui:SettingsCard.HeaderIcon>
                <TextBlock Text="{Binding AutoStartText}" VerticalAlignment="Center"/>
            </ui:SettingsCard>

            <ui:SettingsCard Header="时间校准"
                             HorizontalAlignment="Stretch">
                <ui:SettingsCard.HeaderIcon>
                    <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.ClockToolbox_24_Regular}" />
                </ui:SettingsCard.HeaderIcon>
                <TextBlock Text="{Binding CalibrationText}" VerticalAlignment="Center"/>
            </ui:SettingsCard>

            <TextBlock Text="所有选项之后都可以在设置窗口中随时修改。"
                       FontSize="12"
                       TextAlignment="Center"
                       Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                       Margin="0,16,0,0"/>
        </ikw:SimpleStackPanel>
    </Grid>
</UserControl>